@page
@model ServiceHost.Areas.Administration.Pages.Personnel.PersonGroup.IndexModel
@{
    Layout = "Shared/_AdminLayout";
    ViewData["title"] = "Person Group Management";
}



<div class="row">
    <div class="col-sm-12">
        <h3 class="page-title pull-left">@ViewData["title"]</h3>
        <p class="pull-right">
            <a class="btn btn-success btn-lg" href="#showmodal=@Url.Page("./Index", "Create")" >Create New</a>
        </p>
    </div>
</div>

<div class="row" id="OrganizationGroupDiv">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Organization group list</h3>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-12 col-sm-12 col-xs-12">
                        <table id="datatable" class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    @*<th>Image</th>*@
                                    <th>Description</th>
                                    <th>Remark</th>
                                    <th>Activate</th>
                                    <th>Operation</th>
                                </tr>
                            </thead>
                            <tbody>
                               @foreach (var item in Model.PersonGroups)
                                { 
                                <tr>
                                        <td>@item.Id</td>
                                        <td>@item.Description</td>
                                        <td>@item.Remark</td>
                                        <td>@item.IsActive</td>
                                        <td>
                                            <a class="btn btn-warning pull-left m-rl-5"
                                               href="#showmodal=@Url.Page("./Index", "Edit", new { id = item.Id })">
                                                <i class="fa fa-edit"></i> Edit
                                            </a>
                                        </td>
                                </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/AdminTheme/assets/datatables/jquery.dataTables.min.js"></script>
    <script src="~/AdminTheme/assets/datatables/dataTables.bootstrap.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#datatable').dataTable();
        });
    </script>
}
